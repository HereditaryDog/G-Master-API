# 更新日志

本文档记录 `G-Master API` 对外版本的主要变化。

## v1.0.0-rc.7-GM.1 - 2026-05-22

### 新增

- 新增顶部导航/状态接口的模块开关控制，用户端模块展示会跟随后台配置，同时保留 `Gaster Code` 入口。
- 新增支付合规确认门槛，管理员未确认支付合规前，在线支付入口会被统一隐藏或拒绝创建订单。
- 新增 Waffo Pancake 订阅支付入口，订阅套餐可单独配置 `waffo_pancake_product_id`，支付成功后由同一个 Waffo Pancake webhook 完成订阅订单。
- 渠道亲和新增 `request_header` Key 来源，可按入口请求头生成亲和缓存 key，便于第三方集成按自定义请求头固定渠道。

### 变更

- 模型列表会优先展示启用渠道的供应商归属信息，避免 `owned_by` 与实际可用渠道脱节。
- 性能指标摘要只纳入当前有效分组，同时继续保留使用日志兜底，避免模型健康度在真实调用后仍为空。
- 参数覆写审计扩大到敏感来源路径，覆盖 `input`、`messages`、`system`、`instructions` 等上游请求体字段。
- 运营设置页的渠道亲和编辑器补充请求头来源选项、占位示例和 JSON 模板示例。
- 使用日志的模型、用户名和令牌名筛选改为转义后的 contains 匹配，避免 `%`、`_` 等字符造成误匹配。
- Debug 日志改为按需格式化，生产环境关闭 Debug 时不再提前拼接大请求体日志。

### 修复

- 修复 Waffo Pancake 充值相关路由未启用的问题，并将 webhook 可用性从普通充值商品配置中拆分出来，便于订阅和充值共用基础配置。
- 订阅订单写入充值历史时补充 `payment_provider`，避免后续支付渠道审计信息缺失。
- 修复渠道列表在未输入搜索关键词时忽略分组筛选的问题，类型统计也跟随分组过滤。
- 修复管理员删除用户失败时仍返回成功的问题。
- `/api/status` 补充注册开关，登录页会在关闭注册时隐藏注册入口。
- 管理员新增用户时前端补充 8 到 20 位密码长度校验。

## v1.0.0-rc.5-GM.2 - 2026-05-15

### 修复

- 修复控制台“模型健康度”面板在已有大量模型调用后仍显示空数据的问题。性能指标采集现在默认开启。
- `/api/perf-metrics/summary` 在性能指标聚合表暂时没有数据时，会从现有使用日志按模型汇总请求数、成功率、平均耗时和 TPS，避免新部署或开关缺失时面板空白。
- 当新版本刚开启性能指标、实时采样只有少量新请求时，模型健康度会优先采用更完整的使用日志汇总，避免历史调用被一两条实时样本遮蔽。
- 性能指标摘要接口补充返回 `request_count`，前端可按真实调用量排序模型健康卡片。

## v1.0.0-rc.5-GM.1 - 2026-05-13

### 新增

- 新增性能指标采集能力，包含 `perf_metrics` 聚合表、内存/Redis 热桶、定时落库与 `/api/perf-metrics/summary`、`/api/perf-metrics` 查询接口。
- 新增模型排行接口 `/api/rankings`，基于用量数据计算模型、供应商、涨跌幅和历史份额数据，供模型广场与运营看板使用。
- 控制台首页新增模型健康度卡片，展示 24 小时模型平均延迟、成功率和 TPS。
- 模型广场新增本周模型热度排行条，复用现有 G-Master 前端壳与 Semi UI 风格。
- 日志系统新增供应商 Request ID 采集与查询，relay 响应中的 `X-Oneapi-Request-Id` 会保存到日志字段，管理员可在使用日志中筛选并复制本地/供应商请求 ID。
- 充值页新增钱包与订阅摘要区，集中展示钱包余额、历史消耗、生效订阅、扣费偏好和可用支付入口数量。
- 渠道编辑器新增能力摘要，展示多密钥、模型映射、参数覆写、模型同步状态，并提示轮询多密钥所需的 Redis/内存缓存依赖。

### 变更

- relay 成功消费和最终失败路径会记录性能样本，便于后续定位渠道延迟、成功率和生成吞吐。
- 性能指标采集器默认开启，需要时可通过 `perf_metrics_setting.enabled` 显式关闭。
- 使用日志页保留现有结构，增量补充供应商 Request ID 筛选框和请求 ID 列。
- 钱包、订阅和渠道编辑器继续沿用当前 G-Master 经典前端与支付/编辑流程，仅补充运营可见性和状态汇总。

## v0.13.2-gmaster.6 - 2026-05-13

### 新增

- 新增图片生成异步任务接口：`POST /v1/images/generations/async` 与 `GET /v1/images/jobs/{task_id}`，用于避免 `gpt-image-2` 等长耗时图片生成被 Cloudflare 或反代网关长连接超时截断。

### 变更

- 完成第一轮 v1 RC 兼容修复，保留现有 G-Master 生产前端与品牌页面，不引入大范围前端重写。
- 管理员渠道列表支持服务端排序参数，前端渠道表格会把排序字段传给后端，避免大数据量下只在当前页排序。
- 充值信息接口补充返回 `topup_link`，前端充值入口优先使用用户侧接口返回值，并保留状态接口兜底。

### 修复

- 修复 OpenAI、Claude、Gemini 等转发路径中 reasoning 字段、图片编辑引用、Vertex URL 构造和显式零值参数转发的兼容性问题。
- 修复分层计费价格展示中无缓存 token 时仍展示缓存计费变量的问题。
- 补充兼容更新清单、异步图片任务文档和相关回归测试。

## v0.13.2-gmaster.5 - 2026-05-11

### 修复

- 为 Gaster Code 桌面端登录态失效场景补充稳定的机器可读错误结构，`/api/gaster-code/me`、`/api/gaster-code/provider-token`、`/api/gaster-code/auth/refresh` 和 `/api/gaster-code/auth/revoke` 在登录缺失或会话过期时返回 `code=authentication_failed`、`reason=login_required/session_expired`、`action=relogin` 和 `userMessage`。
- 将无效 refresh token 的响应从通用 `success=false` 调整为 HTTP `401` 的认证错误，便于桌面端区分登录态失效和模型、额度、计费问题。

## v0.13.2-gmaster.4 - 2026-04-29

### 修复

- 修复管理员新增渠道模型或批量更新渠道后，模型广场 `/api/pricing` 可能继续返回旧缓存，导致 DeepSeekv4 等新模型不能立即展示的问题。
- 修复模型价格、倍率、补全、缓存、图片、音频和分层计费相关配置更新后，模型广场缓存未同步失效的问题。
- 为定价缓存失效补充回归测试，覆盖价格配置更新和新渠道新增模型场景。

## v0.13.2-gmaster.3 - 2026-04-29

### 修复

- 修复 Gaster Code 桌面端通过 G-Master API 登录后自动创建的 `Gaster Code Desktop` API Key 没有继承用户当前分组的问题。VIP 用户现在会自动获得 `VIP用户组` 的桌面端专用 Key，普通用户仍跟随自己的普通分组。
- 修复已有桌面端专用 Key 被复用时仍停留在旧空分组的问题。后续桌面端请求 provider token 时，会同步为账号当前分组。

## v0.13.2-gmaster.1 - 2026-04-28

### 变更

- 发布 `v0.13.2` 兼容升级，合入模型同步、分层计费、供应商价格同步、配置解析、Responses 工具调用和 Ali 渠道原生 Messages 相关修复。
- 更新源码构建、Docker 构建、前端展示版本和 Apifox 导出版本号，统一为 `v0.13.2-gmaster.1`。
- 保留当前 G-Master 控制台、模型广场、品牌首页与生产部署视觉体系，暂不引入大范围新前端重写。
- 将站点文档入口接入 Apifox 公开文档，`/docs` 与 `/docs/ai-client` 会 302 跳转到 AI 客户端配置文档，`/docs/openclaw-en` 会跳转到 OpenClaw 英文页。
- 更新 README 与 Apifox 文档包说明，明确站内文档入口只做跳转，不反代 Apifox 内容。

### 新增

- 新增 Gaster Code 桌面端网页登录授权链路，支持通过浏览器登录、PKCE 授权码、本地回调、桌面端 token 和专用 Provider token 完成默认供应商接入。
- 分层计费表达式新增 `len` 条件变量，并补充 LLM 辅助生成提示词，便于管理员按上下文长度配置阶梯计费。
- 供应商价格同步支持从 pricing endpoint 同步分层计费、倍率、补全倍率、缓存、图片和音频价格字段。

### 修复

- 修复 `/login?redirect=/gaster-code/desktop-login?...` 在已有前端 localStorage 登录态时会被直接带到 `/console`，导致桌面端授权页无法继续的问题。
- 修复 OpenAI Responses 转换中工具调用参数为原始 JSON 时的兼容性问题。
- 修复配置更新中 map 字段 JSON 解析不完整的问题，避免部分设置页保存后丢失 map 类型配置。
- 修复供应商模型检查时未加载模型映射的问题，并在模型拉取弹窗中显示已从供应商移除的模型。
- 修复渠道亲和关闭场景下重试提示不清晰的问题。

## v0.13.1-gmaster.1 - 2026-04-24

### 变更

- 发布 `v0.13.1` 兼容升级，补齐 DeepSeek V4 推理后缀处理、分层计费模型列表和计费配置检测修复。
- 补充分层计费表达式能力、动态定价展示、用量日志与倍率设置相关更新，同时保留 `G-Master API` 品牌、生产部署配置、文档入口和现有充值/分组升级逻辑。
- 保留本轮已经落地的控制台数据看板与模型广场局部 UI 改版，并在升级后的基线上继续使用 `v0.13.1-gmaster.1` 作为源码、Docker 构建和前端展示版本。

### 修复

- 修复只配置分层计费表达式的模型可能不出现在模型列表中的问题。
- 修复定价缓存未在 `billing_setting` 更新后失效的问题，避免管理员调整分层计费后前台模型广场继续显示旧配置。
- 修复充值/订阅支付回调按支付方式判断导致的跨网关串单风险，同时允许 EPay 订单在支付页切换实际支付方式后正常完成回调。
- 移除不适用的 nightly DockerHub 发布工作流，避免 G-Master 分支误推送到外部镜像命名空间。

## v0.12.15-gmaster.1 - 2026-04-23

### 变更

- 发布 `v0.12.15` 兼容升级，补齐最新支付、Passkey、Gemini ToolConfig 与 Codex 渠道相关改动，同时保留 `G-Master API` 品牌与现有业务分组/充值升级逻辑。
- 叠加新的控制台数据看板与模型广场局部 UI 改版，保留当前深色微发光视觉方向，同时优化首页摘要、KPI 卡片与供应商头图区块的信息表达。
- 更新源码构建、Docker 构建和 Apifox 导出使用的版本号，统一为 `v0.12.15-gmaster.1`。

### 修复

- 修复 Claude 渠道在处理 OpenAI 风格 inline file 内容时，无法正确区分 `PDF`、纯文本和不支持文件类型的问题；现在 `PDF` 会转为 `document`，文本文件会转为 `text`，未知类型会安全跳过。
- 修复 `StreamScannerHandler` 在已有 `StreamStatus` 预初始化场景下会覆盖原状态的问题，避免已有错误计数和结束状态被意外清空。

## v0.12.14-gmaster.1 - 2026-04-20

### 变更

- 发布 `v0.12.14` 兼容升级，同时保留 `G-Master API` 品牌、现有部署地址与分组充值升级逻辑。
- 新增对 `Claude Opus 4.7` 的支持，并同步更新 Claude / Vertex / AWS 适配常量与相关倍率设置。
- 增强充值日志的管理员审计字段，记录服务器 IP、回调调用者 IP、订单支付方式、回调支付方式与系统版本，便于后台排查补单与支付回调问题。

### 修复

- 增强 SSRF 防护，对内网地址与可疑重定向的拦截更严格。
- 修复管理员禁用、删除、升降级用户后缓存未及时失效的问题，避免用户或令牌在 Redis TTL 到期前继续沿用旧状态。
- 修复管理员调额与强制关闭 2FA 时会把操作者身份直接暴露给普通用户日志页的问题，改为仅管理员可见的展开审计信息。
- 修复充值记录查询可能被恶意模糊搜索拖慢的问题，并将普通用户可查询的充值记录时间窗口限制为最近 `30` 天。
- 修复旧版本实例写入的充值日志在后台展开时没有任何可读提示的问题，管理员现在会看到升级提醒而不是空白面板。

## v0.12.11-gmaster.1 - 2026-04-16

### 变更

- 发布 `v0.12.11` 兼容升级，同时保留 `G-Master API` 品牌、现有部署链接与当前充值升级业务逻辑。
- 补齐 Claude 消息转换的兼容修复，避免 OpenAI 转 Claude 时把空字符串文本块原样透传给供应商。
- 重新生成公开 Apifox 导入文件，确保对外开发文档中的版本号与当前发布线保持一致。

### 修复

- 为 Stripe、Creem、Waffo 和易支付回调补齐 `payment_method` 校验，避免不同支付渠道的订单被错误结算。
- 修复 Creem 创建充值订单时未写入 `payment_method`，导致后续回调无法准确识别渠道来源的问题。

## v0.12.10-gmaster.1 - 2026-04-15

### 变更

- 发布 `v0.12.10` 兼容升级，同时保留 `G-Master API` 品牌、现有部署链接与业务分组策略。
- 补齐 Claude 的 `cache_control` 透传与可选 `speed` 透传能力，并同步更新了管理端配置项与中继请求 schema。
- 重新生成公开的 Apifox 导入文件，确保对外开发文档与当前版本请求结构保持一致。

### 修复

- 补齐 Stripe Checkout 异步支付 webhook 处理逻辑，延迟支付成功或失败时都能正确结算或关闭待处理订单。
- 修复 `/v1/responses` 在供应商返回非字符串或 `null` 类型 `instructions` 字段时的兼容性问题，改为使用 `json.RawMessage` 处理。
- 修复管理员手动调整额度时缺少操作者用户名的问题，并修复多密钥管理弹窗的索引显示从 `#0` 开始的不直观表现。

## v0.12.9-gmaster.1 - 2026-04-14

### 变更

- 发布 `v0.12.9` 兼容升级，同时保留 `G-Master API` 品牌、自定义首页、文档门户与现有业务分组逻辑。
- 补齐仪表盘和设置页的增强，包括更好的图表维度处理、排行逻辑、金额优先的额度调整流程、订阅卡片下次重置时间展示，以及 GroupTable 输入体验修复。
- 重新生成公开 Apifox 导入产物，确保开发文档与新版本以及当前生产地址保持一致。

### 修复

- 修复品牌首页在暗色主题下仍以浅色样式渲染的问题，补齐 `gm-zen-*` 风格集的暗色覆盖。
- 修复公共头部在暗色模式下依然保持亮色栏位的问题，覆盖首页、文档、认证、定价与关于页。
- 补齐 Azure `/v1/responses/compact` 路由、Claude `TopP` 处理、Token 鉴权错误信息收敛，以及 `isStream` 错误日志状态记录的修复。

## v0.12.6-gmaster.3 - 2026-04-14

### 变更

- 统一在线钱包充值完成后的结算路径，使 `epay/alipay/wxpay`、Waffo、Stripe、Creem 与管理员手动完成流程都走同一套额度结算和用户组升级逻辑。
- 将 root 用户初始化分组策略与普通注册逻辑拆开，保证首次超级管理员初始化仍进入 `VIP用户组`，而普通注册用户进入 `标准用户组`。
- 更新默认 group ratio 与 top-up ratio 的兜底映射，同时兼容旧的 `default/vip/svip` 键与当前的 `标准用户组 / VIP用户组` 业务分组。

### 修复

- 修复线上 `epay` 充值成功后只增加额度、不升级到 `VIP用户组` 的问题。
- 修复会话中的用户组缓存滞后问题，避免已升级用户或超级管理员仍以旧分组权限访问。
- 修复分组相关的定价、模型与分组接口读取了登录时缓存而不是当前持久化分组的问题。

## v0.12.6-gmaster.2 - 2026-04-13

### 变更

- 统一新用户入组逻辑，密码注册、管理员创建账号、OAuth 首次登录以及 root 初始化都默认进入 `标准用户组`。
- 统一充值成功后的升级逻辑，使钱包充值一旦完成，就立即升级到 `VIP用户组`，不再区分充值金额。

### 修复

- 修复新创建用户仍可能落回旧数据库默认分组，而不是当前业务分组配置的问题。
- 修复充值成功与用户组升级之间存在时间差的问题，将升级动作放入同一事务，并在完成后刷新缓存分组。

## v0.12.6-gmaster.1 - 2026-04-09

### 变更

- 发布 `v0.12.6` 兼容升级，同时保留 `G-Master API` 首页、文档门户、品牌名称与对外文案。
- 补齐新的设置页与价格管理改版，包括新的分组倍率管理流程及相关管理端界面更新。
- 补齐仪表盘增强，包括更强的数据分析视图、图表排序与坐标轴修复，以及 API 信息面板中的复制动作。
- 重新生成公开 Apifox 导入产物，使公共开发文档与当前发布版本对齐。

### 新增

- 新增 `MiniMax` 图像生成中继的支持。
- 新增 SMTP 强制 `AUTH LOGIN` 的支持，以兼容更严格的邮件服务商。
- 新增显式模型名匹配的渠道亲和规则支持。

### 修复

- 修复仪表盘统计卡片内部内容对齐不稳定的问题。
- 修复历史兼容更新过程中遗留的模块路径和示例域名，使品牌分支统一回到 `G-Master API` 与 `gmapi.fun`。

## v0.12.1-gmaster.4 - 2026-04-09

### 变更

- 更新公开首页的模型能力文案，使其描述稳定能力类别，而不是短期变化的模型名称。
- 更新首页 SDK 示例与公开文档代码片段，统一改用更中性的 `your-chat-model` 占位符和当前 OpenAI SDK 写法。
- 清理公开 `/docs` 页面与 Apifox 源页面中的内部发布措辞，使其更像生产环境下的开发者文档门户。

## v0.12.1-gmaster.3 - 2026-04-09

### 变更

- 将当前 `G-Master API` 栈发布到腾讯云生产环境 `https://gmapi.fun`，同时保留本地 Docker 作为评审优先的修改方式。
- 更新仓库发布元信息与生成文档资产，确保公开版本号在 UI、README 和 Apifox 导入文件中保持一致。

### 修复

- 修复 Playground 桌面布局中模型配置面板与全局侧边栏冲突的问题。
- 修复多密钥轮询警告总是显示的问题，使其真实反映运行时缓存状态。
- 在 `/api/status` 中公开 `redis_enabled` 与 `memory_cache_enabled`，便于前端正确判断缓存能力。

## v0.12.1-gmaster.2 - 2026-04-08

### 变更

- 将公开首页重建为 `Zen-AI` 风格的落地页，同时保留 `G-Master API` 品牌、链接和部署流程。
- 统一控制台与模型定价页的视觉系统，更新渐变、玻璃卡片、圆角控件与整体间距。
- 刷新共享头部、侧边栏、Logo 表现、页面背景和操作区域，让主要产品面更统一。
- 新增首页插图资源，并更新默认 Logo 的缓存破坏参数以便发布切换。

### 修复

- 修复主题刷新后仪表盘统计图标丢失的问题，重新补足头像图标的前景色。
- 修复首页副标题打字动画会挤压 `G-Master API` 标题的位置问题。
- 修复首页副标题宽度预留不足导致中文文案尾部被裁切的问题。

## v0.12.1-gmaster.1 - 2026-04-06

### 变更

- 发布 `v0.12.1` 兼容基线。
- 刷新 `gmapi.fun` 的部署资产，并新增页脚版本号展示支持。
